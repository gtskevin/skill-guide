---
description: "Generate a shareable portfolio HTML page of your skill stack"
argument-hint: "[--user <name>]"
---

Run the share command:

```bash
node <skill-dir>/skill-guide.js --share --open --user "<name>"
```

If no `--user` is provided, the page is generated without attribution. The output is a standalone HTML file suitable for sharing.
