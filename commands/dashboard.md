---
description: "Generate an HTML dashboard of all installed agent skills with categories, health radar, and review candidates"
---

Run the default dashboard command:

```bash
node <skill-dir>/skill-guide.js --refresh --open
```

Add `--lang zh` for Chinese. Add `--all` to see skills from all platforms (default: current platform only). Summarize the generated result and report the output path.
